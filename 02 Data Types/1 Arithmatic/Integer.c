#include <stdio.h>
#include <limits.h>

int main()
{	
	printf("Storage   Size  : %ld byte \n", sizeof(int));
	printf("Minimum   Value : %d byte \n", INT_MIN);
	printf("Maximum   Value : %d byte \n", INT_MAX);
	return 0;
}
